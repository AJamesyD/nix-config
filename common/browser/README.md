# Browser Config

Per-browser modules imported by `default.nix`. Each subdirectory is
a self-contained home-manager module for one browser.

| Directory | Browser | Binary managed by |
|---|---|---|
| `zen/` | [Zen Browser](https://zen-browser.app) | Homebrew cask |
