#!/usr/bin/env bash
# ~/.bin/pbpaste
set -eo pipefail
if test -n "$SSH_TTY"; then
  nc -c -w1 localhost 2225
elif test "$(uname)" = Darwin; then
  /usr/bin/pbpaste
else
  xsel --clipboard --output
fi
