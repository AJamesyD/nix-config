.direnv
.devenv

# Symlink to nix store, managed by devenv:git-hooks:install.
# If stale: rm this file and re-enter the shell (see git-hooks.nix #685).
.pre-commit-config.yaml

nix.conf
registry.json

nixos.qcow2

# Compiled event provider binaries
hosts/m3-work-laptop/sketchybar/helpers/event_providers/*/bin/
.kiro/
